# {{.ProjectName}}

A gRPC Gateway service that provides both gRPC and REST API interfaces for modern microservices architecture.

## Overview

This service implements the gRPC Gateway pattern for {{.ModulePath}}.

## Features

- gRPC Server on port {{.GrpcPort}}
- REST Gateway on port {{.HttpPort}}
- Structured Logging with {{.Logger}}
- Go {{.GoVersion}}

## Quick Start

```bash
go mod tidy
make generate
make run
```

The service will start on ports {{.GrpcPort}} (gRPC) and {{.HttpPort}} (REST).