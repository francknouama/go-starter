package models

import (
	"time"
)

// BaseModel provides common fields for all models
type BaseModel struct {
	ID        uint      `json:"id" {{if eq .DatabaseORM "gorm"}}gorm:"primaryKey"{{end}}`
	CreatedAt time.Time `json:"created_at" {{if eq .DatabaseORM "gorm"}}gorm:"autoCreateTime"{{end}}`
	UpdatedAt time.Time `json:"updated_at" {{if eq .DatabaseORM "gorm"}}gorm:"autoUpdateTime"{{end}}`
}