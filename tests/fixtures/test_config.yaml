profiles:
  test:
    author: "Test Author"
    email: "test@example.com"
    license: "MIT"
    defaults:
      go_version: "1.21"
      framework: "gin"
      architecture: "standard"
      database:
        driver: "postgres"
        orm: "gorm"
      auth:
        type: "jwt"
  
  minimal:
    author: "Minimal User"
    email: "minimal@example.com"
    license: "Apache-2.0"
    defaults:
      go_version: "1.20"
      framework: "echo"
      architecture: "clean"
      database:
        driver: ""
        orm: "sqlx"
      auth:
        type: ""

current_profile: "test"